{% extends 'base.html.twig' %}

{% block title %}Hello SecurityController!
{% endblock %}

{% block body %}
	<h1>connexion</h1>
	{% if errors %}
		<div class="alert alert-danger">{{errors.message}}</div>
	{% endif %}
	{{form_start(formView)}}
	{{form_widget(formView)}}
	<button class="btn btn-success" type="submit">Accedere</button>
	{{form_end(formView)}}

{% endblock %}
