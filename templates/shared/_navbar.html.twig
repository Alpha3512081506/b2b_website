<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="/">RicomputerB2BShop</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor03">
		<ul class="navbar-nav mr-auto">

			{% for category in categories.findAll() %}
				<li class="nav-item active">
					<a class="nav-link" href="{{path('prodotto_categorie',{"slug":category.slug })}}">
						<font style="vertical-align: inherit;">
							<font style="vertical-align: inherit;">
								{{category.nomeCategoria}}
							</font>
						</font>
						<span class="sr-only">
							<font style="vertical-align: inherit;">
								<font style="vertical-align: inherit;">
									(actuel)
								</font>
							</font>
						</span>
					</a>
				</li>
			{% endfor %}


		</ul>
		<ul class="navbar-nav ">
			<li class="nav-item">
				<a href="{{path('cart_show')}}" class="nav-link">
					<i class="fa fa-shopping-cart"></i>carrello
																																																																																																																							({{cartService.total}})
				</a>
			</li>
			{% if app.user %}
				<li class="nav-item">
					<a href="{{path("purchase_index")}}" class="nav-link">Miei Ordini</a>
				</li>
				<li class="nav-item">
					<a href="{{path('security_logout')}}" class="btn btn-danger">Logout</a>
				</li>
			{% else %}
				<li class="nav-item">
					<a href="{{path('account_register')}}" class="nav-link">Register</a>
				</li>
				<li class="nav-item">
					<a href="{{path('security_login')}}" class="btn btn-success">Login</a>
				</li>


			{% endif %}

		</ul>

	</div>
</nav>
